# Mock Exam 3 - Question 11

Create a Horizontal Pod Autoscaler (HPA) `api-hpa` for the deployment named `api-deployment` located in the `api` namespace.
The HPA should scale the deployment based on a custom metric named `requests_per_second`, targeting an average value of 1000 requests per second across all pods.
Set the minimum number of replicas to 1 and the maximum to 20.

Note: Deployment named `api-deployment` is available in api namespace. Ignore errors due to the metric `requests_per_second` not being tracked in `metrics-server`

## Topics Tested

- **Horizontal Pod Autoscaler (HPA)**: Automatically scaling based on metrics
- **HPA v2 API**: Using autoscaling/v2 for advanced metric types
- **Custom Metrics**: Scaling based on application-specific metrics
- **Pods Metric Type**: Using per-pod metrics vs resource metrics
- **AverageValue Target**: Setting target values for custom metrics
- **Custom Metrics API**: Understanding the custom metrics pipeline
- **Metrics Server vs Custom Metrics**: Distinguishing resource and custom metrics
- **HPA Configuration**: Setting min/max replicas and metric targets

### Official Documentation

- [Horizontal Pod Autoscaling](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/)
- [HPA with Custom Metrics](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale-walkthrough/#autoscaling-on-multiple-metrics-and-custom-metrics)
- [HPA API Reference (v2)](https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/horizontal-pod-autoscaler-v2/)
- [Metrics APIs](https://kubernetes.io/docs/tasks/debug/debug-cluster/resource-metrics-pipeline/#metrics-api)

## Sample Solution

Under /root/ folder you will find a yaml file `api-hpa.yaml`. Update the yaml file as per task given.

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-hpa
  namespace: api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-deployment
  minReplicas: 1
  maxReplicas: 20
  metrics:
  - type: Pods
    pods:
      metric:
        name: requests_per_second
      target:
        type: AverageValue
        averageValue: "1000"
```

Use below command

```bash
kubectl create -f api-hpa.yaml
```