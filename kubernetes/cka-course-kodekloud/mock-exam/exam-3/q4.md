# Mock Exam 3 - Question 4

Create a ConfigMap named `app-config` in the namespace `cm-namespace` with the following key-value pairs:

```bash
ENV=production
LOG_LEVEL=info
```

Then, modify the existing Deployment named `cm-webapp` in the same namespace to use the `app-config` ConfigMap by setting the environment variables `ENV` and `LOG_LEVEL` in the container from the ConfigMap.

## Topics Tested

- **ConfigMaps**: Creating ConfigMaps to store non-confidential configuration data
- **ConfigMap Creation**: Using --from-literal flags for key-value pairs
- **Environment Variables from ConfigMaps**: Injecting ConfigMap data as environment variables
- **kubectl set env**: Updating deployment environment variables imperatively
- **Deployment Updates**: Modifying existing deployments without downtime
- **Configuration Management**: Separating configuration from application code
- **Namespaces**: Working with resources in specific namespaces

### Official Documentation

- [ConfigMaps](https://kubernetes.io/docs/concepts/configuration/configmap/)
- [Configure a Pod to Use a ConfigMap](https://kubernetes.io/docs/tasks/configure-pod-container/configure-pod-configmap/)
- [Define Environment Variables for a Container](https://kubernetes.io/docs/tasks/inject-data-application/define-environment-variable-container/)
- [kubectl set env](https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands#-em-env-em-)

## Sample Solution

Use kubectl create configmap to create the ConfigMap and kubectl set env to modify the deployment to use the ConfigMap.

Create the ConfigMap:

```bash
kubectl create configmap app-config -n cm-namespace \
  --from-literal=ENV=production \
  --from-literal=LOG_LEVEL=info
```

Patch the deployment to use the config:
```bash
kubectl set env deployment/cm-webapp -n cm-namespace \
  --from=configmap/app-config
```